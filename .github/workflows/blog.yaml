name: blog
on: push
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build phase
        run: echo "building source code"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test phase
        run: echo "running tests"

  deloy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: deploy phase
        run: echo "deploying the code to aws"
